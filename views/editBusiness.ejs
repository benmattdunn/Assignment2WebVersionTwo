<% include partials/header.ejs%>
<!-- I'm so tired at this point, this is really shittily done.-->
<form method="post">
    <div class="form-group">
        <label for="businessname" class="col-sm-2 control-label">Name:</label>
        <input name="businessname" id="businesname" required value="<%= business.businessname %>" />
    </div>
    <div class="form-group">
        <label for="businesstags" class="col-sm-2 control-label">Tags:</label>
        <input name="businesstags" id="businestags" required value="<%= business.businesstags %>" />
    </div>
    <div class="form-group">
        <label for="businessdescription" class="col-sm-2 control-label">description:</label>
        <input name="businessdescription" id="businessdescription" required value="<%= business.businessdescription %>" />
    </div>
    <div class="form-group">
        <label for="businessaddress" class="col-sm-2 control-label">address:</label>
        <input name="businessaddress" id="businessaddress" required value="<%= business.businessaddress %>" />
    </div>
    <button class="btn btn-success col-sm-offset-2">Save</button>
</form>

<% include partials/footer.ejs %>